//! Placeholder entry point for the `syauth` binary.
//!
//! Roadmap items S-011 (`pair`), S-012 (`list`/`revoke`/`status`), and S-013
//! (`install-pam`/`uninstall-pam`) replace this body with real subcommands.
//! For now the binary exits 0 so that `cargo build --workspace` succeeds.

fn main() {}
